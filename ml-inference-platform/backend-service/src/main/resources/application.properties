# MongoDB Configuration
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://db_user_test:unioptimum123@cluster0.mg17igq.mongodb.net/?appName=Cluster0}

# Spring gRPC Configuration (Spring gRPC 1.0.0+)
# Server Configuration (if this service exposes gRPC)
spring.grpc.server.port=9090

# Client Configuration
# Assuming we name the client 'inference-client' or similar to match the stub injection
# Target the Inference Service host and port defined in docker-compose
spring.grpc.client.channels.inferenceService.address=static://${INFERENCE_SERVICE_HOST:inference-service}:${INFERENCE_SERVICE_PORT:50051}
spring.grpc.client.channels.inferenceService.negotiation-type=plaintext
